interface:
  display_name: "Rehydrate Session"
  short_description: "Recover and verify context during execution"
  default_prompt: "Use $rehydrate:rehydrate to recover relevant session history and recheck prior evidence during execution whenever it could change the next action."
policy:
  allow_implicit_invocation: false
