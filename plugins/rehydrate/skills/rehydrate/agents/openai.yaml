interface:
  display_name: "Rehydrate Session"
  short_description: "Recover context with bounded evidence queries"
  default_prompt: "Use $rehydrate:rehydrate to recover the task trajectory with a bounded structural index and targeted evidence expansion."
policy:
  allow_implicit_invocation: false
